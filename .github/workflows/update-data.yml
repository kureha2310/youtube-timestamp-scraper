name: Update Timestamp Data

on:
  # 毎日 AM 9:00 (JST = UTC 0:00) に自動実行
  schedule:
    - cron: '0 0 * * *'

  # 手動実行も可能
  workflow_dispatch:

  # プッシュ時にもテスト実行（mainブランチのみ）
  push:
    branches:
      - main
    paths:
      - 'src/**'
      - 'export_to_web.py'

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r config/requirements.txt

      - name: Run scraper for all channels
        env:
          API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
        run: |
          python -c "
          from src.extractors.youtube_song_scraper import main
          from src.utils.channel_manager import get_enabled_channels

          channels = get_enabled_channels()
          print(f'スクレイピング対象: {len(channels)}チャンネル')

          for ch in channels:
              print(f\"処理中: {ch['name']}...\")
              try:
                  main(channel_id=ch['channel_id'])
              except Exception as e:
                  print(f\"エラー: {ch['name']} - {e}\")
                  continue
          "

      - name: Export data to JSON
        env:
          API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
        run: |
          python export_to_web.py

      - name: Commit and push if changed
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🤖 自動更新: タイムスタンプデータ"
          commit_user_name: "github-actions[bot]"
          commit_user_email: "github-actions[bot]@users.noreply.github.com"
